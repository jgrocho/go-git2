*   Alphabetize definitions.

*   Use varagrs where appropriate (e.g. flags).

*   For functions which return struct pointers, if the git struct inside
    the struct is NULL, the function should return nil.

Search for TODO: comments in the source.
